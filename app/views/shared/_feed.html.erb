<% if @feed_items.any? %>
  <ol class="microposts">
  <!--- each object of @feed_items belongs to  class Micropost, hence will look for partial app/views/microposts/_micropost.html.erb --->
     <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
